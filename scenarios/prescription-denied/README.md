# prescription-denied

A prescription gets denied - that item is cancelled before sourcing runs, while the rest of the order ships normally.

`input.json` / `output.json` are generated by this scenario's last run - see `dotnet run --project src/JevWf.Evaluation -- prescription-denied`.
