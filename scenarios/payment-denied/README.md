# payment-denied

Payment never clears for the item's seller - cancelled before any classification gate or sourcing runs.

`input.json` / `output.json` are generated by this scenario's last run - see `dotnet run --project src/JevWf.Evaluation -- payment-denied`.
