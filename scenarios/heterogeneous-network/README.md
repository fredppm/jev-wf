# heterogeneous-network

Five items, five origin types (express hub, customs dock, regional warehouse, made-to-order factory, cold DC), all three SLAs at once.

`input.json` / `output.json` are generated by this scenario's last run - see `dotnet run --project src/JevWf.Evaluation -- heterogeneous-network`.
