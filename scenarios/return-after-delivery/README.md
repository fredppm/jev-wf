# return-after-delivery

A delivered item is returned - the one post-delivery path (Returned + refund) no other scenario reaches.

`input.json` / `output.json` are generated by this scenario's last run - see `dotnet run --project src/JevWf.Evaluation -- return-after-delivery`.
