# mixed-attributes

Regression case: prescription + digital + cold-chain + queue-effect sourcing + a deferred item, all in one order.

`input.json` / `output.json` are generated by this scenario's last run - see `dotnet run --project src/JevWf.Evaluation -- mixed-attributes`.
