# same-origin-merges-across-sellers

Two items from different sellers that route through the same origin+SLA must merge into one shipment.

`input.json` / `output.json` are generated by this scenario's last run - see `dotnet run --project src/JevWf.Evaluation -- same-origin-merges-across-sellers`.
