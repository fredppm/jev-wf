# simple-shirt

One item, one seller, standard shipping. Trivial baseline, all the way to Delivered.

`input.json` / `output.json` are generated by this scenario's last run - see `dotnet run --project src/JevWf.Evaluation -- simple-shirt`.
